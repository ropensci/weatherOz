% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_weather_summary.R
\name{tidy_weather_summary}
\alias{tidy_weather_summary}
\title{Individual station summaries nicely formatted.}
\usage{
tidy_weather_summary(
  site = NULL,
  first,
  last = Sys.Date(),
  api_key = NULL,
  interval = c("daily", "15min", "30min", "hourly", "monthly", "yearly"),
  api_name = "weather",
  api_version = 2,
  which_vars = c("all", "rain", "wind", "temp", "erosion")
)
}
\arguments{
\item{site}{A string of the station ID code for the station of interest.
Passed through from \code{get_summaries()}}

\item{first}{A string representing the start date of the query in the
format 'yyyy-mm-dd'.}

\item{last}{A string representing the start date of the query in the
format 'yyyy-mm-dd'.}

\item{api_key}{Api key from DPIRD (https://www.agric.wa.gov.au/web-apis).
Defaults to NULL.}

\item{interval}{Time interval to summarise over.
Default is 'daily'; others are '15min', '30min', 'hourly',
'monthly', 'yearly'.For intervals shorter than 1 day, time period covered
will be midnight to midnight, with the last time interval being before
midnight - hour/minute values are for the end of the time period.
Data for shorter intervals ('15min', '30min') are available from
January of the previous year.}

\item{api_name}{Defaults to "weather", Only works with DPIRD's Weather API.}

\item{api_version}{Defaults to 2, and gives a error if not 2.}

\item{which_vars}{Match weather summary selected. Defaults to "all".
Can be one of "all", "rain", "wind", "temp" and "erosion."}
}
\value{
a \verb{data frame} with site and date interval queried together with
}
\description{
Individual station summaries nicely formatted.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# You must have an DPIRD API key to proceed
mykey <- 'dpird_api_key'

# Set date interval for yearly request
# Get rainfall summary
start_date <- "2017-10-28"

# Use default for end data (current system date)
output <- tidy_weather_summary(
            site = "CL001",
            first = start_date,
            api_key = mykey,
            interval = "yearly",
            which_vars = "rain")

# Only for wind and erosion conditions for daily time interval
# define start and end date
start_date <- "2022-05-01"
end_date <- "2022-05-02"

output <- tidy_weather_summary(
            site = "BI",
            first = start_date,
            last = end_date,
            api_key = mykey,
            interval = "daily",
            which_vars = c("wind", "erosion"))
\dontshow{\}) # examplesIf}
}
\seealso{
Other DPIRD: 
\code{\link{get_extreme_weather}()},
\code{\link{get_summaries}()}
}
\concept{DPIRD}
